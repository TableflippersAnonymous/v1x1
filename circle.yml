dependencies:
  override:
  - make -e -j dependencies
test:
  override:
  - make -e -j test
machine:
  services:
  - docker
deployment:
  prod:
    branch: master
    codedeploy:
      v1x1:
        deployment_group: prod
        deployment_config: CodeDeployDefault.AllAtOnce
