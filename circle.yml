dependencies:
  override:
  - make -j dependencies
test:
  override:
  - make -j test
machine:
  services:
  - docker
deployment:
  prod:
    branch: master
    codedeploy:
      v1x1:
        deployment_group: prod
        deployment_config: CodeDeployDefault.AllAtOnce
